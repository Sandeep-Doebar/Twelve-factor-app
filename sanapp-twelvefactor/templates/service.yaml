apiVersion: v1
kind: Service
metadata:
  name: {{ include "sanapp-twelvefactor.fullname" . }}
  labels:
    {{- include "sanapp-twelvefactor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sanapp-twelvefactor.selectorLabels" . | nindent 4 }}
