apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sanapp-twelvefactor.fullname" . }}-test-connection"
  labels:
    {{- include "sanapp-twelvefactor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sanapp-twelvefactor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
